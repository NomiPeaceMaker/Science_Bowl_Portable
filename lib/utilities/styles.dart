//This document contains all styles that appear more than once in the application

import 'package:flutter/material.dart';

final disableColor = Colors.grey[400];

final themeColor = Color(0xFFF8B400);

final appBarStyle = TextStyle(
  color: Colors.white,
  fontFamily: 'Roboto',
);

final exitstyle = TextStyle(
  color: Colors.red,
  fontSize: 16.0,
);

final staystyle = TextStyle(
  color: Colors.grey[700],
  fontSize: 16.0,
);

final loginButtonText = TextStyle(
  color: Colors.white,
  // fontWeight: FontWeight.bold,
  fontSize: 21.0,
);